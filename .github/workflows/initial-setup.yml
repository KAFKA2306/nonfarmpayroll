name: Initial Repository Setup (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Explain canonical publication path
        run: |
          set -euo pipefail
          echo "::notice::This workflow is retired because the legacy dashboard can publish unverified revision data."
          echo "Use .github/workflows/update-dashboard.yml for the canonical fail-closed Pages deployment."
